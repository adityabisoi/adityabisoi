name: Update README

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "Hello ${{ github.event.inputs.name }}"
    - name: Test update to repo
      run: |
        echo "Test push commit to the remote in the workflow" >> test.txt

        git config --global user.name "Aditya Bisoi"
        git config --global user.email "adityabisoi@users.noreply.github.com"

        git add -A
        git commit -m "Update test.txt"
        git push